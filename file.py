# coding: utf-8

import numpy as np
a = np.array([1, 2, 3])
b = np.array([[3, 3], [6, 6][9, 9]])
b = np.array([[3, 3], [6, 6], [9, 9]])
a * b
np.transpose(a) * b
np.transpose(a)
np.transpose(a)
np.transpose
np.transpose.__doc__
print(np.transpose.__doc__)
np.transpose(a, axis=0)
np.transpose(a, axis=1)
np.transpose(a, 0)
np.transpose(a, 1)
np.reshape(1, 3)
np.reshape((1, 3))
print(np.reshape.__doc__)
np.reshape(a, (1, 3))
np.reshape(a, (0, 3))
a.T
a
b
b.T
a * b.T
get_ipython().magic('clear ')
a
b
a * b.T
b
b.T
b
b = a * b.T
b
b = b.T
b
get_ipython().magic('clear ')
get_ipython().magic('save')
get_ipython().magic('save file')
get_ipython().magic('save file 0-40')
